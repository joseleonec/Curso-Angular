let name: string = "Strider";
const msg = "Hello, TypeScript";
let hpPoints: number | String = 100;
let isLive: boolean = true;

// Array
let skills: string[] = ["Bash", "Counter", "Healing"];
let hp: number[] = [100, 200, 300];

console.log(name, msg, hpPoints, isLive, skills, hp);

export { };
